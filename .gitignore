# these are left-over from running notebooks, and should not be tracked
.ipynb*

# no reason to track these
*.pyc

# build folder generated by setup tools
build/

# data folder is intended as scratch space for running scripts, so results
# should not be tracked; the run_me.sh script, however, should be tracked.
data/
!data/run_me.sh
